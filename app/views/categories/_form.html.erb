<%= form_for(@category) do |form| %>
  <p><%= form.label :category %></p>
  <p><%= form.text_field :name, placeholder: "Category name." %></p>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>